import React from 'react';

export function Contact({pageBag}) {

    return(

        <div>
            <h1 className="font-sans text-red text-4xl text-center pt-12">{pageBag.description}. Styled with Tailwind</h1>
        </div>
    );
}
